package pt

/*
func BenchmarkRandomVec(b *testing.B) {
	b.Run("New", func(b *testing.B) {
		r := rand.New(rand.NewSource(time.Now().UnixNano()))
		b.ResetTimer()
		for i := 0; i < b.N; i++ {
			RandomUnitVector(r)
		}
	})

	b.Run("Old", func(b *testing.B) {
		r := rand.New(rand.NewSource(time.Now().UnixNano()))
		b.ResetTimer()
		for i := 0; i < b.N; i++ {
			RandomUnitVectorOld(r)
		}
	})
}
*/
